/**
 * Returns true if value is an image extension or mime type
 * @param { string } value
 * @returns { boolean }
 */
declare function isImage(value: string | undefined): boolean;
export default isImage;
